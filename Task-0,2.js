let x = 1 + 1 * 2;
let y = (1 + 1) * 2;
let z = 1 + (1 *2);
let a = 1 + 1 * 2 / 2;
let b = (1 + 1 * 2) / 2;

answers = [x, y, z, a, b];
for (i=0; i < answers.length; i++){
    console.log(answers[i]);
}